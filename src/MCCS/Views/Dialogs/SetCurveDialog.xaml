<UserControl x:Class="MCCS.Views.Dialogs.SetCurveDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.Views.Dialogs"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance Type=local:SetCurveDialog}">
    <UserControl.Resources>
        <Thickness x:Key="TextBottomThickness">0,0,0,5</Thickness>
        <Thickness x:Key="StackPanelBottomThickness">0,0,0,10</Thickness>
    </UserControl.Resources>
    <GroupBox
        Width="500"
        Height="Auto"
        Margin="16"
        Header="设置曲线"
        Style="{StaticResource MaterialDesignCardGroupBox}"> 
        <StackPanel>
            <StackPanel Margin="{StaticResource StackPanelBottomThickness}">
                <TextBlock Text="Y轴" Margin="{StaticResource TextBottomThickness}" />
                <ComboBox>
                    <ComboBoxItem Content="通道1试验力"></ComboBoxItem>
                    <ComboBoxItem Content="通道1位移"></ComboBoxItem>
                    <ComboBoxItem Content="通道2试验力"></ComboBoxItem>
                    <ComboBoxItem Content="通道2位移"></ComboBoxItem>
                </ComboBox>
            </StackPanel>
            <StackPanel Margin="{StaticResource StackPanelBottomThickness}">
                <TextBlock Text="X轴" Margin="0,0,5,0" />
                <ComboBox>
                    <ComboBoxItem Content="时间"></ComboBoxItem>
                    <ComboBoxItem Content="通道1位移"></ComboBoxItem>
                </ComboBox>
            </StackPanel>
            <Separator Margin="{StaticResource StackPanelBottomThickness}"></Separator>
            <StackPanel  HorizontalAlignment="Center" Orientation="Horizontal">
                <Button Content="确定" Style="{StaticResource MaterialDesignRaisedSecondaryButton}" Margin="0,0,15, 0"></Button>
                <Button Content="取消" Command="{Binding CancelCommand}"></Button>
            </StackPanel>
        </StackPanel>
    </GroupBox>
</UserControl>
