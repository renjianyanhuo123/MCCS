<UserControl x:Class="MCCS.Views.Dialogs.Hardwares.EditHardwareDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.Views.Dialogs.Hardwares"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:hardwares="clr-namespace:MCCS.Converters.Hardwares"
             xmlns:prism="http://prismlibrary.com/" xmlns:hardwares1="clr-namespace:MCCS.ViewModels.Dialogs.Hardwares" 
             d:DataContext="{d:DesignInstance Type=hardwares1:EditHardwareDialogViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <hardwares:DeviceTypeEnumToStringConverter x:Key="DeviceTypeEnumToStringConverter" />
    </UserControl.Resources>
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction 
                Command="{Binding LoadCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid Height="600" Width="800">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" BorderThickness="0,0,0,2" BorderBrush="#C5C5C5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="80"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="编辑设备"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="20"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"></TextBlock>
                <Viewbox Grid.Column="1" Width="30" Height="30" 
                 Margin="20"
                 HorizontalAlignment="Right"
                 Cursor="Hand">
                    <b:Interaction.Triggers>
                        <b:EventTrigger EventName="MouseDown">
                            <b:InvokeCommandAction 
                        Command="{Binding CloseCommand}" />
                        </b:EventTrigger>
                    </b:Interaction.Triggers>
                    <materialDesign:PackIcon Kind="Close" />
                </Viewbox>
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Column="0" Grid.Row="0" Margin="15">
                    <TextBlock Text="设备名称" FontSize="14" Margin="0,0,0,15"></TextBlock>
                    <TextBox Width="Auto" Text="{Binding DeviceName}"></TextBox>
                </StackPanel>
                <StackPanel Grid.Column="1" Grid.Row="0" Margin="15">
                    <TextBlock Text="设备类型" FontSize="14" Margin="0,0,0,15"></TextBlock>
                    <ComboBox  Width="Auto" SelectedIndex="{Binding DeviceType}">
                        <ComboBoxItem TabIndex="0">未知</ComboBoxItem>
                        <ComboBoxItem TabIndex="1">传感器</ComboBoxItem>
                        <ComboBoxItem TabIndex="2">作动器</ComboBoxItem>
                        <ComboBoxItem TabIndex="3">控制器</ComboBoxItem>
                        <ComboBoxItem TabIndex="4">网关</ComboBoxItem>
                        <ComboBoxItem TabIndex="5">服务器</ComboBoxItem>
                        <ComboBoxItem TabIndex="6">其他</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Margin="15">
                    <TextBlock Text="描述" FontSize="14" Margin="0,0,0,15"></TextBlock>
                    <TextBox Text="{Binding Desprition}"></TextBox>
                </StackPanel>
                <StackPanel Grid.Column="0" Grid.Row="2"
                        Grid.ColumnSpan="2"
                    Margin="15" Orientation="Vertical">
                    <TextBlock Text="功能分类" FontSize="14" Margin="0,0,0,15"></TextBlock>
                    <ComboBox Width="Auto" 
                      Height="Auto"
                      SelectedIndex="{Binding FunctionType}">
                        <ComboBoxItem TabIndex="0">执行类</ComboBoxItem>
                        <ComboBoxItem TabIndex="1">测量类</ComboBoxItem>
                        <ComboBoxItem TabIndex="2">连接类</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </ScrollViewer>
        <Border Grid.Row="2" Grid.Column="0" 
        BorderThickness="0,2,0,0" BorderBrush="#C5C5C5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button 
            Content="取消" 
            Width="80"
            Command="{Binding CloseCommand}"
            Style="{StaticResource MaterialDesignOutlinedSecondaryButton}" />
                <Button
            Width="80"
            Content="提交" 
            Margin="30,0,30,0"
            Command="{Binding SaveCommand}"
            Style="{StaticResource MaterialDesignFlatMidBgButton}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
