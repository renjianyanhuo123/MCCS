<UserControl x:Class="MCCS.Views.MethodManager.Contents.MethodWorkflowSettingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.Views.MethodManager.Contents"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- 滚动区域 -->
        <ScrollViewer Name="CanvasScrollViewer"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto" 
                      Background="LightGray">
            <Canvas Name="DrawingCanvas" 
                    Background="White"
                    Width="Auto" 
                    Height="2000">
                <Canvas.RenderTransform>
                    <ScaleTransform x:Name="CanvasScaleTransform" 
                                    ScaleX="1" ScaleY="1" 
                                    CenterX="0" CenterY="0"/>
                </Canvas.RenderTransform>

                <Rectangle Fill="Red" Width="100" Height="100" Canvas.Left="50" Canvas.Top="50"/>
                <Rectangle Fill="Blue" Width="100" Height="100" Canvas.Left="200" Canvas.Top="200"/>
                <Rectangle Fill="Green" Width="100" Height="100" Canvas.Left="400" Canvas.Top="400"/>
                <Rectangle Fill="Orange" Width="100" Height="100" Canvas.Left="600" Canvas.Top="600"/>
                <Rectangle Width="2000" Height="2000" Fill="Transparent" Stroke="LightBlue" StrokeThickness="1" StrokeDashArray="10,10"/>
            </Canvas>
        </ScrollViewer>

        <!-- 固定在顶部的工具栏（覆盖在 ScrollViewer 上方） -->
        <StackPanel VerticalAlignment="Top"
                    Opacity="0.9"
                    Background="White"
                    Orientation="Horizontal"
                    Panel.ZIndex="999">
            <materialDesign:NumericUpDown 
                Margin="30,10,10,10" 
                materialDesign:TextFieldAssist.SuffixText="%" 
                Value="100"
                Maximum="100" Minimum="0" />
        </StackPanel>
    </Grid>
</UserControl>
