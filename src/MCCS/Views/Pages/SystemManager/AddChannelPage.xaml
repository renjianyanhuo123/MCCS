<UserControl x:Class="MCCS.Views.Pages.SystemManager.AddChannelPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.Views.Pages.SystemManager"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:systemManager="clr-namespace:MCCS.ViewModels.Pages.SystemManager"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1080" d:DataContext="{d:DesignInstance Type=systemManager:AddChannelPageViewModel }">
    <GroupBox
        Margin="16"
        Height="Auto"
        Width="500"
        materialDesign:ElevationAssist.Elevation="Dp8"
        VerticalAlignment="Center" 
        HorizontalAlignment="Center"
        BorderThickness="0"
        Header="添加通道"
        Style="{StaticResource MaterialDesignGroupBox}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"></RowDefinition>
                <RowDefinition Height="60"></RowDefinition>
                <RowDefinition Height="60"></RowDefinition>
                <RowDefinition Height="60"></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                <materialDesign:PackIcon Kind="Asterisk" Width="8" Height="8" />
                <TextBlock  Text="通道名称："></TextBlock>
            </StackPanel>
            <TextBox Grid.Row="0" Grid.Column="1" materialDesign:HintAssist.Hint="请输入通道名称" Height="30" Text="{Binding ChannelName, Mode=TwoWay}"></TextBox>
            <TextBlock VerticalAlignment="Center"  HorizontalAlignment="Right"  Grid.Row="1" Grid.Column="0" Text="是否可见："></TextBlock>
            <ToggleButton HorizontalAlignment="Left"  Grid.Row="1" Grid.Column="1" IsChecked="{Binding IsShowable, Mode=TwoWay}"></ToggleButton>
            <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Text="是否开启试样保护："></TextBlock>
            <ToggleButton HorizontalAlignment="Left"  Grid.Row="2" Grid.Column="1" IsChecked="{Binding IsOpenProtected, Mode=TwoWay}"></ToggleButton>
            <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Width="80" Content="保存" Command="{Binding AddChannelCommand}"></Button>
            <materialDesign:Snackbar  x:Name="SnackbarSeven"  MessageQueue="{materialDesign:MessageQueue}"  />
        </Grid>
    </GroupBox>

</UserControl>
