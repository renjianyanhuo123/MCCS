<UserControl x:Class="MCCS.Views.Pages.SystemManager.HardwareSettingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:systemmanager="clr-namespace:MCCS.ViewModels.Pages.SystemManager" 
             d:DataContext="{d:DesignInstance Type=systemmanager:HardwareSettingPageViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction 
                Command="{Binding LoadCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <UniformGrid Columns="2"> 
                <materialDesign:Chip
                    Content="添加通道">
                    <materialDesign:Chip.Icon>
                        <materialDesign:PackIcon Kind="Plus" />
                    </materialDesign:Chip.Icon>
                </materialDesign:Chip>
                <materialDesign:Chip
                    Content="添加变量">
                    <materialDesign:Chip.Icon>
                        <materialDesign:PackIcon Kind="Plus" />
                    </materialDesign:Chip.Icon>
                </materialDesign:Chip> 
            </UniformGrid>
            <TreeView>
                <ItemsControl ItemsSource="{Binding ChannelVariableInfo}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TreeViewItem>
                                <TreeViewItem.Header>
                                    <StackPanel Orientation="Horizontal" Tag="{Binding ChannelId}">
                                        <Viewbox Width="16" Height="16">
                                            <materialDesign:PackIcon Kind="Pipe" />
                                        </Viewbox>
                                        <TextBlock Margin="8,0,0,0" Text="{Binding ChannelName}" />
                                    </StackPanel>
                                </TreeViewItem.Header>
                                <ItemsControl ItemsSource="{Binding VariableInfos}">
                                    <!-- <materialDesign:PackIcon Kind="Variable" /> -->
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TreeViewItem Tag="{Binding VariableId}" Header="{Binding VariableName}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </TreeViewItem>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl> 
            </TreeView>
        </StackPanel>
    </Grid>
</UserControl>
