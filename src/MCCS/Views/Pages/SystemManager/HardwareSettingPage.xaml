<UserControl x:Class="MCCS.Views.Pages.SystemManager.HardwareSettingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:systemmanager="clr-namespace:MCCS.ViewModels.Pages.SystemManager" 
             d:DataContext="{d:DesignInstance Type=systemmanager:HardwareSettingPageViewModel}"
             xmlns:localContanst="clr-namespace:MCCS"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:MCCS.Behaviors"
             xmlns:hardwares="clr-namespace:MCCS.Converters.Hardwares"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.PopupBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBlock.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <hardwares:DeviceTypeEnumToStringConverter x:Key="DeviceTypeEnumToStringConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction 
                Command="{Binding LoadCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Left"
                    Margin="15,0,0,0"
                    VerticalAlignment="Center">
            <Button
                Width="100"
                Content="添加设备" 
                Command="{Binding AddHardwareCommand}"
                Style="{StaticResource MaterialDesignFlatLightBgButton}" />
        </StackPanel>
        <DataGrid
            Grid.Row="1"
            Name="HardwareDataGrid"
            CanUserAddRows="False"
            ColumnWidth="*" 
            ItemsSource="{Binding HardwareList}" 
            AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Visibility="Hidden"></DataGridTextColumn>
                <DataGridTextColumn Header="设备名称" Binding="{Binding Name}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="设备类型" Binding="{Binding Type, Converter={StaticResource DeviceTypeEnumToStringConverter}}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="描述" Binding="{Binding Description}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="创建时间" IsReadOnly="True" Binding="{Binding CreateTime}" Width="*"/>
                <DataGridTextColumn Header="更新时间" IsReadOnly="True" Binding="{Binding UpdateTime}" Width="*"/>
                <DataGridTemplateColumn Header="操作"  Width="350">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="编辑" Width="80" 
                                        Click="EditButton_Click"
                                        Tag="{Binding Id}"
                                        Style="{StaticResource MaterialDesignFlatMidBgButton}"  />
                                <Button Content="编辑信号接口" Width="120" 
                                        Margin="10,0,0,0" 
                                        Click="EditSignalButton_Click"
                                        Tag="{Binding Id}"
                                        Style="{StaticResource MaterialDesignFlatSecondaryLightBgButton}"  />
                                <Button Content="删除" Width="80"
                                        Margin="10,0,0,0" 
                                        Click="DeleteButton_Click"
                                        Style="{StaticResource MaterialDesignPaperLightButton}"
                                        Tag="{Binding Id}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn> 
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
