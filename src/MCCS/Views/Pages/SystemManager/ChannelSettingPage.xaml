<UserControl x:Class="MCCS.Views.Pages.SystemManager.ChannelSettingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:systemmanager="clr-namespace:MCCS.ViewModels.Pages.SystemManager" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800" d:DataContext="{d:DesignInstance Type=systemmanager:ChannelSettingPageViewModel}">
    <UserControl.Resources>
        <!-- 定义数据模板 -->
        <DataTemplate x:Key="HardwareItemTemplate">
            <Border BorderBrush="LightGray" BorderThickness="5" Margin="2" Padding="8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions> 

                    <TextBlock Grid.Column="0" Text="{Binding HardwareName}"></TextBlock>
                    <TextBlock Grid.Column="1" Text="{Binding ControllerName}"></TextBlock>
                    <materialDesign:PackIcon Grid.Column="2" Kind="DeleteForever" Tag="{Binding HardwareId}"></materialDesign:PackIcon>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- 定义ItemsControl的样式 -->
        <Style x:Key="ListViewStyle" TargetType="ItemsControl">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ItemsControl">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                CornerRadius="5"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Focusable="False">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 列表项容器样式 -->
        <!--<Style x:Key="ListItemStyle" TargetType="ContentPresenter">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
        </Style>-->
    </UserControl.Resources>
    <Grid>
         <Grid.RowDefinitions>
             <RowDefinition Height="80" />
             <RowDefinition Height="*" />
             <RowDefinition Height="150" />
         </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal">
            <StackPanel Orientation="Horizontal" Width="150" VerticalAlignment="Center" Margin="15,0,0,0">
                <TextBlock Text="通道名称" VerticalAlignment="Center" Margin="0,0,10,0"></TextBlock>
                <TextBox materialDesign:HintAssist.Hint="输入通道名称"  />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Width="150" VerticalAlignment="Center" Margin="15,0,0,0">
                <TextBlock Text="是否可见" VerticalAlignment="Center" Margin="0,0,10,0"></TextBlock>
                <ToggleButton></ToggleButton>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Width="180" VerticalAlignment="Center" Margin="15,0,0,0">
                <TextBlock Text="内部名称" Margin="0,0,10,0"></TextBlock>
                <TextBlock Text="channel_0299jjiks"></TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Width="200" VerticalAlignment="Center" Margin="15,0,0,0">
                <TextBlock Text="是否开启试样保护" Margin="0,0,10,0" VerticalAlignment="Center"></TextBlock>
                <ToggleButton></ToggleButton>
            </StackPanel>
        </StackPanel>

        <ItemsControl Grid.Column="0" Grid.Row="1" 
                      ItemsSource="{Binding ChannelHardwareInfo}"
                      Style="{StaticResource ListViewStyle}"
                      ItemTemplate="{StaticResource HardwareItemTemplate}"
                      Margin="5">
            <!-- 自定义ItemsPanel使用VirtualizingStackPanel提高性能 -->
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Grid>
</UserControl>
