<UserControl x:Class="MCCS.Views.Pages.SystemManager.EditModelScenarioSettingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.Views.Pages.SystemManager"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:systemmanager="clr-namespace:MCCS.ViewModels.Pages.SystemManager"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             d:DataContext="{d:DesignInstance Type=systemmanager:EditModelScenarioSettingPageViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="1.2*"></RowDefinition>
            <RowDefinition Height="1.1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="250"></ColumnDefinition>
         </Grid.ColumnDefinitions>
        <StackPanel 
            Grid.Column="0" 
            Grid.Row="0" 
            HorizontalAlignment="Right" 
            Margin="0,0,30,0" 
            VerticalAlignment="Center" 
            Grid.ColumnSpan="2" 
            Height="Auto">
            <Button Width="80" Content="保存"></Button>
        </StackPanel>
    </Grid>
</UserControl>
