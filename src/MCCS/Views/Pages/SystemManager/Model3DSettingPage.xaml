<UserControl x:Class="MCCS.Views.Pages.SystemManager.Model3DSettingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.Views.Pages.SystemManager"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:systemmanager="clr-namespace:MCCS.ViewModels.Pages.SystemManager"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             d:DataContext="{d:DesignInstance Type=systemmanager:Model3DSettingPageViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction 
                Command="{Binding LoadCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
                    Orientation="Vertical" 
                    HorizontalAlignment="Left" 
                    Margin="10"
                    VerticalAlignment="Center">
            <Button Width="100" Content="新增模型" Command="{Binding AddModelCommand}"></Button>
        </StackPanel>
        <DataGrid
            Grid.Row="1"
            CanUserAddRows="False"
            ColumnWidth="*"
            ItemsSource="{Binding Model3Ds}"
            SelectionMode="Extended"
            AutoGenerateColumns="False"
            SelectionUnit="Cell" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Visibility="Hidden"></DataGridTextColumn>
                <DataGridTextColumn Header="模型名称" Binding="{Binding Name}" Width="*"/>
                <DataGridTemplateColumn Header="是否正在使用" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ToggleButton 
                                IsChecked="{Binding IsUse, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn> 
                <DataGridTextColumn Header="创建时间" Binding="{Binding CreateTime}" Width="*"/>
                <DataGridTextColumn Header="更新时间" Binding="{Binding UpdateTime}" Width="*"/>
                <DataGridTemplateColumn Header="操作"  Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="编辑模型" Width="100"></Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
