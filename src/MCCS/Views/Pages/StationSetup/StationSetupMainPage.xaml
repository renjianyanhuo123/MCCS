<UserControl x:Class="MCCS.Views.Pages.StationSetup.StationSetupMainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.Views.Pages.StationSetup"
             xmlns:stationsetup="clr-namespace:MCCS.ViewModels.Pages.StationSetup"
             xmlns:regions="http://prismlibrary.com/"
             xmlns:mccs="clr-namespace:MCCS"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             d:DataContext="{d:DesignInstance Type=stationsetup:StationSetupMainPageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction 
                Command="{Binding LoadCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ListBox
            Grid.Row="0"
            Grid.Column="0" 
            ItemsSource="{Binding MenuItems}"
            SelectedItem="{Binding SelectedMenuItem}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" 
                               FontSize="14" 
                               Margin="15,5,5,5" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ContentControl Grid.Row="0" Grid.Column="1" regions:RegionManager.RegionName="{x:Static mccs:GlobalConstant.StationSetupRegionName}" />
    </Grid>
</UserControl>
