<UserControl x:Class="MCCS.Views.Pages.HomeTestOperationPage"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Canvas x:Name="ExternalContainer" ClipToBounds="True">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="MouseRightButtonDown">
                <i:InvokeCommandAction Command="{Binding MouseRightButtonDownCommand}"
                                       CommandParameter="{Binding ElementName=ExternalContainer}"/>
            </i:EventTrigger>
            <i:EventTrigger EventName="MouseMove">
                <i:InvokeCommandAction Command="{Binding MouseMoveCommand}"
                                       CommandParameter="{Binding ElementName=WidgetCanvas}" />
            </i:EventTrigger>
            <i:EventTrigger EventName="MouseRightButtonUp">
                <i:InvokeCommandAction Command="{Binding MouseRightButtonUpCommand}"
                                       />
            </i:EventTrigger>
            <i:EventTrigger EventName="MouseWheel">
                <i:InvokeCommandAction Command="{Binding MouseWheelCommand}" PassEventArgsToCommand="True"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <Canvas x:Name="WidgetCanvas" 
                Width="400"
                Height="300"
                Canvas.Left="50"
                Canvas.Top="50"
                Background="AliceBlue">
            <Canvas.RenderTransform>
                <MatrixTransform x:Name="OperationTransform" />
            </Canvas.RenderTransform>
        </Canvas>
    </Canvas>
</UserControl>
