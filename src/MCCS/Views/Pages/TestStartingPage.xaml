<UserControl x:Class="MCCS.Views.Pages.TestStartingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.Views.Pages"
             xmlns:helix="clr-namespace：HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:h="http://helix-toolkit.org/wpf"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <TextBlock Text="当前选中模型: " VerticalAlignment="Center"/>
            <TextBlock x:Name="SelectedModelText" VerticalAlignment="Center" Margin="5,0" Text="无"/>
        </StackPanel>

        <h:HelixViewport3D x:Name="viewPort" Grid.Row="1" 
                               ZoomExtentsWhenLoaded="True"
                               ShowCoordinateSystem="True"
                               Background="LightGray"
                               MouseDown="ViewPort_MouseDown"
                               MouseMove="ViewPort_MouseMove">
            <h:HelixViewport3D.Camera>
                <PerspectiveCamera Position="10,10,10" LookDirection="-10,-10,-10" UpDirection="0,0,1"/>
            </h:HelixViewport3D.Camera>
            <h:SunLight/>
            <ModelVisual3D x:Name="ModelsGroupContainer" />
        </h:HelixViewport3D>
    </Grid>
</UserControl>
