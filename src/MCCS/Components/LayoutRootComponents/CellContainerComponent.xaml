<UserControl x:Class="MCCS.Components.LayoutRootComponents.CellContainerComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.Components.LayoutRootComponents"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance local:CellContainerComponentViewModel}">
    <Grid>
        <!-- 你的容器内容（底层） -->
        <Border CornerRadius="8" >
            <Grid>
                <!-- 这里放你的真实内容 -->
                <ContentControl x:Name="ComponentElementRegion" />
            </Grid>
        </Border>
        <Button Width="36" Height="36"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,8,8,0" 
                Padding="0"
                BorderThickness="0"
                Cursor="Hand"
                ToolTip="弹出"
                Command="{Binding PopupCommand}"
                CommandParameter="{Binding ElementName=ComponentElementRegion}">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="bd"
                                CornerRadius="18"
                                Background="#66FFFFFF"  
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12"
                                                  ShadowDepth="2"
                                                  Opacity="0.25"/>
                            </Border.Effect>
                        </Border>
                        <materialDesign:PackIcon HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"
                                                 Foreground="Black"
                                                 Kind="ApplicationOutline" />
                    </Grid>
                    
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#99FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#BBFFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>
