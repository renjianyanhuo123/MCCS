<UserControl x:Class="MCCS.Interface.Components.Views.Parameters.SetControlOperationParamPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:parameters="clr-namespace:MCCS.Interface.Components.ViewModels.Parameters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="450"
             d:DataContext="{d:DesignInstance parameters:SetControlOperationParamPageViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <!-- 通道设置列表 -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ChannelSettings}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="Border"
                                Padding="12"
                                BorderBrush="{DynamicResource MaterialDesignDivider}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Background="Transparent"
                                Margin="8,4"
                                Cursor="Hand">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 通道标题 --> 
                                <CheckBox Grid.Row="0" FontSize="16"
                                          FontWeight="Bold"
                                          Foreground="{DynamicResource PrimaryHueMidBrush}"
                                          Margin="0,0,0,8"
                                          IsChecked="{Binding Selected}"
                                          Content="{Binding Name}"></CheckBox>

                                <!-- 控制模式多选 -->
                                <StackPanel Grid.Row="1">
                                    <TextBlock FontSize="14"
                                               Text="允许的控制方式:"
                                               Margin="0,0,0,8"
                                               Foreground="{DynamicResource MaterialDesignBody}"/>
                                    <ItemsControl ItemsSource="{Binding ControlModeOptions}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <UniformGrid Columns="2" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox Content="{Binding DisplayName}"
                                                          IsChecked="{Binding IsSelected}"
                                                          FontSize="13"
                                                          Margin="0,4"
                                                          Style="{StaticResource MaterialDesignCheckBox}"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Grid>
                        </Border>
                        <DataTemplate.Triggers>
                            <Trigger Property="Border.IsMouseOver" Value="True">
                                <Setter TargetName="Border"
                                        Property="Background"
                                        Value="{DynamicResource MaterialDesignDivider}"/>
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 保存/取消按钮 -->
        <UniformGrid Grid.Row="1" Columns="2" Margin="8">
            <Button Width="100"
                    Height="40"
                    Content="保存"
                    Command="{Binding SaveCommand}"
                    Margin="0,0,8,0"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
            <Button Width="100"
                    Height="40"
                    Content="取消"
                    Margin="8,0,0,0"
                    Style="{DynamicResource MaterialDesignOutlinedSecondaryLightButton}"/>
        </UniformGrid>
    </Grid>
</UserControl>
