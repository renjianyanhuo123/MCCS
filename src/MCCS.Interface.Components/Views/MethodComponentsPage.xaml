<UserControl x:Class="MCCS.Interface.Components.Views.MethodComponentsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:viewModels="clr-namespace:MCCS.Interface.Components.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:MethodComponentsPageViewModel}">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction 
                Command="{Binding LoadCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <ScrollViewer>
        <ItemsControl
            Grid.IsSharedSizeScope="True"
            ItemsSource="{Binding GroupModels}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card>
                        <Expander HorizontalAlignment="Stretch" Header="{Binding GroupName}">
                            <ItemsControl ItemsSource="{Binding Items}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border
                                                x:Name="Border"
                                                Padding="8"
                                                BorderBrush="{DynamicResource MaterialDesignDivider}"
                                                BorderThickness="0,0,0,1"
                                                Background="Transparent"
                                                Cursor="Hand">  
                                            <Border.InputBindings>
                                                <MouseBinding 
                                                        MouseAction="LeftClick"
                                                        Command="{Binding DataContext.SelectComponentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" />
                                            </Border.InputBindings>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition SharedSizeGroup="Checkerz" />
                                                    <ColumnDefinition />
                                                </Grid.ColumnDefinitions>
                                                <Button 
                                                        Grid.Column="0"
                                                        Width="40"
                                                        Height="40"
                                                        Style="{StaticResource MaterialDesignFloatingActionSecondaryButton}"
                                                        ToolTip="{Binding Title}" 
                                                        IsHitTestVisible="False">
                                                    <materialDesign:PackIcon
                                                        Width="20"
                                                        Height="20"
                                                        Kind="{Binding IconStr}" />
                                                </Button>
                                                <StackPanel
                                                        Grid.Column="1"
                                                        Margin="8,0,0,0">
                                                    <TextBlock
                                                            FontWeight="Bold"
                                                            FontSize="15"
                                                            Text="{Binding Title}" />
                                                    <!--<TextBlock Text="{Binding Description}" />-->
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                        <DataTemplate.Triggers>
                                            <Trigger Property="Border.IsMouseOver" Value="True">
                                                <Setter
                                                    TargetName="Border"
                                                    Property="Background"
                                                    Value="{DynamicResource MaterialDesignDivider}" />
                                            </Trigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </materialDesign:Card>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl> 
    </ScrollViewer>
</UserControl>
