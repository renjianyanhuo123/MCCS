<UserControl x:Class="MCCS.WorkflowSetting.Components.WorkflowDecisionNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.WorkflowSetting.Components"
             xmlns:localViewModel="clr-namespace:MCCS.WorkflowSetting.Models.Nodes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converters="clr-namespace:MCCS.Common.Resources.Converters;assembly=MCCS.Common.Resources"
             xmlns:converters1="clr-namespace:MCCS.WorkflowSetting.Resources.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="200" d:DataContext="{d:DesignInstance localViewModel:DecisionNode}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"></BooleanToVisibilityConverter>
        <converters:ReverseBooleanToVisibilityConverter x:Key="ReverseBooleanToVisibilityConverter" />
        <converters1:BooleanToColorConverter x:Key="BooleanToColorConverter" />
    </UserControl.Resources>
    <Grid Width="{Binding Width}" Height="{Binding Height}">  
        <Border BorderBrush="#CCCCCC"
                Background="Transparent"
                HorizontalAlignment="Left" 
                Margin="{Binding BorderLeftMargin}"
                BorderThickness="0,1.5,0,1.5"
                Width="{Binding BorderWidth}"
                Height="{Binding BorderHeight}"
                Visibility="{Binding IsCollapse,Converter={StaticResource BooleanToVisibilityConverter}}" />
        <ItemsControl ItemsSource="{Binding Children}"
                      Visibility="{Binding IsCollapse,Converter={StaticResource BooleanToVisibilityConverter}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"  />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="FrameworkElement.Margin" 
                            Value="{Binding DataContext.ItemMargin, 
                            RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding}" VerticalAlignment="Top">
                        <!--<ContentControl.InputBindings>
                            <MouseBinding MouseAction="LeftClick" 
                                          Command="{Binding DataContext.NodeClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          CommandParameter="{Binding}" />
                        </ContentControl.InputBindings>-->
                    </ContentControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <!-- 中心节点：旋转的Border内含图片 -->
        <Grid Grid.Row="0"
              Margin="0,-17,0,0"
              Width="100"
              Height="34"
              VerticalAlignment="Top"
              Background="Transparent"
              HorizontalAlignment="Center">
            <i:Interaction.Triggers> 
                <i:EventTrigger EventName="MouseLeave">
                    <prism:InvokeCommandAction Command="{Binding MouseLeaveCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0"
                    Width="20"
                    Height="20"
                    VerticalAlignment="Center"
                    x:Name="LeftBorder"
                    ToolTip="添加分支"
                    Visibility="{Binding IsShowOperationBtn, Converter={StaticResource BooleanToVisibilityConverter}}"
                    CornerRadius="50" >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction Command="{Binding AddBranchCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#CCCCCC"/>
                        <Setter Property="Cursor" Value="Hand" />
                    </Style>
                </Border.Style>
                <!-- 加号图片 -->
                <Image Source="/MCCS.WorkflowSetting;component/Resources/plus.png"
                       Width="16" Height="16"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center" />
            </Border>
            <Border 
                Grid.Column="1"
                Width="34" 
                Height="34"  
                CornerRadius="5" 
                Background="{Binding IsCollapse, Converter={StaticResource BooleanToColorConverter}}"
                Cursor="Hand"
                RenderTransformOrigin="0.5,0.5">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseEnter">
                        <prism:InvokeCommandAction Command="{Binding MouseEnterCommand}" 
                                                   CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Button}}"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <prism:InvokeCommandAction Command="{Binding DeleteAllDecisionCommand}" 
                                                   CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Button}}"/>
                    </i:EventTrigger>
                    <!--DeleteAllDecisionCommand-->
                </i:Interaction.Triggers>
                <Border.Effect>
                    <DropShadowEffect Color="Gray" 
                                      Opacity="0.2" 
                                      BlurRadius="10" 
                                      ShadowDepth="2"/>
                </Border.Effect>
                <Border.RenderTransform>
                    <RotateTransform Angle="45"/>
                </Border.RenderTransform> 
                <Grid>
                    <Image Width="Auto" Height="Auto" Visibility="{Binding IsCollapse, Converter={StaticResource BooleanToVisibilityConverter}}"
                           Margin="6" Source="/MCCS.WorkflowSetting;component/Resources/ellipse.png" />
                    <TextBlock Text="{Binding DecisionNum}" FontSize="24"
                               Foreground="White"
                               Width="Auto" Height="Auto" 
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               RenderTransformOrigin="0.5,0.5"
                               Visibility="{Binding IsCollapse, Converter={StaticResource ReverseBooleanToVisibilityConverter}}">
                        <TextBlock.RenderTransform>
                            <RotateTransform Angle="-45"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Grid>
            </Border>
            <Border Grid.Column="2"
                    x:Name="RightBorder"
                    Width="20" 
                    Height="20"
                    ToolTip="收起" 
                    Visibility="{Binding IsShowOperationBtn, Converter={StaticResource BooleanToVisibilityConverter}}"
                    VerticalAlignment="Center"
                    CornerRadius="50" >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction Command="{Binding CollapseCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#CCCCCC"/>
                        <Setter Property="Cursor" Value="Hand" />
                    </Style>
                </Border.Style>
                <!-- 收起图片 -->
                <Image Source="/MCCS.WorkflowSetting;component/Resources/chevronup_16.png" 
                       Width="16" Height="16" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" /> 
            </Border>
        </Grid>
    </Grid>
</UserControl>
