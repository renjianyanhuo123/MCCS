<UserControl x:Class="MCCS.WorkflowSetting.Components.WorkflowTempPlaceholderAddNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.WorkflowSetting.Components"
             xmlns:nodes="clr-namespace:MCCS.WorkflowSetting.Models.Nodes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance nodes:TempPlaceholderAddNode}">
    <UserControl.Resources>
        <!-- 卡片样式 -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="25,25"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect 
                        Color="#4A90E2" 
                        BlurRadius="80" 
                        ShadowDepth="0" 
                        Opacity="0.6"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 文本样式 -->
        <Style x:Key="MainTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
        </Style>
    </UserControl.Resources>
    <Border Width="{Binding Width}"
            Height="{Binding Height}"
            Style="{StaticResource CardStyle}"
            MaxWidth="500">

        <TextBlock Style="{StaticResource MainTextStyle}">
            <Run Text="选择要执行的动作"/>
            <!--<Run Text="复制已有节点" Style="{StaticResource LinkTextStyle}" 
                 Foreground="#4A90E2" TextDecorations="None"/>-->
        </TextBlock>

    </Border>
</UserControl>
