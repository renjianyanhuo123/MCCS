<UserControl x:Class="MCCS.WorkflowSetting.Components.WorkflowStartNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.WorkflowSetting.Components"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="120">
    <UserControl.Resources>
        <!-- 开始节点样式 -->
        <Style x:Key="StartNodeStyle" TargetType="Border">
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Height" Value="Auto"/>
            <Setter Property="CornerRadius" Value="60"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#4CAF50"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#CCCCCC" 
                                      BlurRadius="10" 
                                      ShadowDepth="2" 
                                      Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#45A049"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#4CAF50" 
                                              BlurRadius="15" 
                                              ShadowDepth="2" 
                                              Opacity="0.5"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 文字样式 -->
        <Style x:Key="StartTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#4CAF50"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <Grid Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}" 
          Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <!-- 开始节点 -->
        <Border Style="{StaticResource StartNodeStyle}" 
                Cursor="Hand">
            <TextBlock Text="开始" 
                       Style="{StaticResource StartTextStyle}"/>
        </Border>
    </Grid>
</UserControl>
