<UserControl x:Class="MCCS.WorkflowSetting.Components.WorkflowEndNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCCS.WorkflowSetting.Components"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="112">
    <Grid  Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}" 
           Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Source="/MCCS.WorkflowSetting;component/Resources/WorkflowEnd.png" />
        <TextBlock Grid.Row="1" Text="流程结束" 
                   FontSize="14" 
                   Margin="0,5,0,0"
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center" />
    </Grid>
</UserControl>
