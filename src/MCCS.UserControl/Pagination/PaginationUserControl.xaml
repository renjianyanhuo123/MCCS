<UserControl x:Class="MCCS.UserControl.Pagination.PaginationUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MCCS.UserControl.Pagination"
             xmlns:assert="clr-namespace:MCCS.UserControl.Assert"
             xmlns:customComboBox="clr-namespace:MCCS.UserControl.ComboBox"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="1000">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding WindowLoadedCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MCCS.UserControl;component/Styles/CustomComboBoxStyle.xaml"/>
                <ResourceDictionary Source="/MCCS.UserControl;component/Styles/CustomTextBox.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <!-- Border Style -->
            <Style x:Key="PageBorderStyle" TargetType="Border">
                <Setter Property="Height" Value="28" />
                <Setter Property="Width" Value="28" />
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="BorderBrush" Value="#D9D9D9"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="5"/>
                <Setter Property="Margin" Value="0 0 8 0"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="#2DB7F5" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            <!-- TextBlock Style -->
            <Style x:Key="PageTextBlockStyle" TargetType="TextBlock">
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="FontFamily" Value="Arial"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="#666666"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType=Border}}" Value="True">
                        <Setter Property="Foreground" Value="#2DB7F5" />
                    </DataTrigger>
                </Style.Triggers>

            </Style>
            <!-- Normal TextBox Style -->
            <Style x:Key="PageNormalTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Arial"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="#666666"/>
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="Transparent">
        
        <StackPanel 
            Orientation="Horizontal" 
            VerticalAlignment="Center"
            Background="Transparent"
            Width="{Binding ActualWidth, ElementName=root}"
            Height="{Binding ActualHeight, ElementName=root}">
            <StackPanel x:Name="TotalStackPanel"
                        Orientation="Horizontal" 
                        Margin="8 0 8 0">
                <TextBlock Style="{StaticResource PageNormalTextStyle}" Text="共" />
                <TextBlock 
                    x:Name="TotalPageText"
                    Style="{StaticResource PageNormalTextStyle}" 
                    Margin="5 0 5 0" 
                    FontWeight="Bold" />
                <TextBlock Style="{StaticResource PageNormalTextStyle}" Text="条" />
            </StackPanel>
            <Border Style="{StaticResource PageBorderStyle}">
                <Image Width="16" Height="16" Source="/MCCS.UserControl;component/Assert/left_32.png" />
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="1" Style="{StaticResource PageTextBlockStyle}" />
            </Border> 
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="2" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="3" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="4" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="5" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Width="16" Height="Auto" Margin="0 0 8 0">
                <Image Source="/MCCS.UserControl;component/Assert/omitPoint_16.png" />
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="6" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="7" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="8" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="9" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="10" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Width="16" Height="Auto" Margin="0 0 8 0">
                <Image Source="/MCCS.UserControl;component/Assert/omitPoint_16.png" />
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="11" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="12" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="13" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}" Visibility="Collapsed">
                <TextBlock Text="14" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <TextBlock Text="15" Style="{StaticResource PageTextBlockStyle}"/>
            </Border>
            <Border Style="{StaticResource PageBorderStyle}">
                <Image Width="16" Height="16" Source="/MCCS.UserControl;component/Assert/right_32.png" />
            </Border>
            <ComboBox 
                Style="{StaticResource CustomComboBoxWithItemStyle}"
                Width="78" 
                Height="28" 
                Margin="5 0 15 0">
                <ComboBoxItem>1</ComboBoxItem>
                <ComboBoxItem>2</ComboBoxItem>
                <ComboBoxItem>3</ComboBoxItem>
                <ComboBoxItem>4</ComboBoxItem>
            </ComboBox>
            <StackPanel Orientation="Horizontal">
                <TextBlock 
                    Width="Auto" 
                    Height="Auto" 
                    Margin="0 0 8 0"
                    Style="{StaticResource PageNormalTextStyle}" 
                    Text="跳转"></TextBlock>
                <TextBox 
                    Width="50" 
                    Height="30"
                    VerticalContentAlignment="Center"
                    Margin="0 0 8 0"
                    ></TextBox>
                <TextBlock Text="页"
                           Width="Auto" 
                           Height="Auto" 
                           Margin="0 0 8 0"
                           Style="{StaticResource PageNormalTextStyle}"></TextBlock>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
