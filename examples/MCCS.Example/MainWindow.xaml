<Window x:Class="MCCS.Example.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MCCS.Example"
        xmlns:transferControl="clr-namespace:MCCS.UserControl.Transfer;assembly=MCCS.UserControl"
        xmlns:pagination="clr-namespace:MCCS.UserControl.Pagination;assembly=MCCS.UserControl"
        xmlns:navigation="clr-namespace:MCCS.Controls.Controls.Navigation;assembly=MCCS.Controls"
        xmlns:views="clr-namespace:MCCS.Interface.Components.Views;assembly=MCCS.Interface.Components"
        xmlns:vm="clr-namespace:MCCS.Interface.Components.ViewModels;assembly=MCCS.Interface.Components"
        xmlns:controlOperationComponents="clr-namespace:MCCS.Interface.Components.ViewModels.ControlOperationComponents;assembly=MCCS.Interface.Components"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="1000">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type controlOperationComponents:ControlOperationComponentPageViewModel}">
            <views:ControlOperationComponentPage />
        </DataTemplate>
    </Window.Resources>
    <Grid >
        <!--
        使用示例:
        Total: 总页数
        DefaultPageSize: 默认每页显示条数
        DefaultCurrentPage: 默认当前页
        PageChangedCommand: 页码改变时触发的命令
        注意: 因为是自定义控件, 所以需要在使用的地方添加xmlns:customPagination="clr-namespace:MCCS.UserControl.Pagination;assembly=MCCS.UserControl"
        1. ShowTotal: 是否显示总页数
        2. 一定要写明使用的哪一个DataContext,不然会默认使用控件自身的DataContext
        -->
        <!--<pagination:PaginationUserControl 
            x:Name="PaginationUserControl" 
            ShowTotal="True"
            Width="1000" 
            Height="30" 
            Total="{Binding DataContext.Total, RelativeSource={RelativeSource AncestorType=local:MainWindow}}"
            DefaultPageSize="10"
            DefaultCurrentPage="6"
            PageChangedCommand="{Binding DataContext.OnPageChanged, RelativeSource={RelativeSource AncestorType=local:MainWindow}}" />-->
        <!--<transferControl:TransferUserControl Width="Auto" Height="Auto" 
                                             SourceItems="{Binding SourceList}"
                                             TargetItems="{Binding TargetList}"
                                             SourceName="{Binding SourceName}" TargetName="{Binding TargetName}"/>--> 
        <!-- 工具栏 -->
        <!--
        <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#E0E0E0" 
                BorderThickness="0,0,0,1" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Name="btnRender" Content="渲染工作流" 
                        Width="100" Height="30" Margin="5"
                        Click="BtnRender_OnClick"
                        Background="#2196F3" Foreground="White"
                        BorderThickness="0" Cursor="Hand"/>
                <Button Name="btnClear" Content="清空画布" 
                        Width="100" Height="30" Margin="5"
                        Background="#FF5722" Foreground="White"
                        BorderThickness="0" Cursor="Hand"/>
                <TextBlock Text="工作流可视化设计器" 
                          VerticalAlignment="Center" 
                          Margin="20,0,0,0"
                          FontSize="14" FontWeight="Bold"
                          Foreground="#333333"/>
            </StackPanel>
        </Border>

        -->
        <!-- 滚动视图 -->
        <!--
        <ScrollViewer Grid.Row="1" 
                     HorizontalScrollBarVisibility="Auto" 
                     VerticalScrollBarVisibility="Auto"
                     Background="#FAFAFA">
            -->
        <!-- 画布 -->
        <!--
            <Canvas Name="workflowCanvas" 
                   Width="2000" 
                   Height="2000"
                   Background="White">
                -->
        <!-- 网格背景（可选） -->
        <!--
                <Canvas.Resources>
                    <Style TargetType="Canvas">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" 
                                                        BlurRadius="20" 
                                                        Opacity="0.1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Canvas.Resources>
            </Canvas>
        </ScrollViewer>-->
        <!--<navigation:CurvedTabControl Width="800" Height="50">
            <navigation:CurvedTabItem Header="测试" />
            <navigation:CurvedTabItem Header="方法" />
            <navigation:CurvedTabItem Header="报告" />
        </navigation:CurvedTabControl>-->
        <ContentControl Content="{Binding }" />
    </Grid>
</Window>
